Call ./get-closure.sh

Determine the version number of the file just downloaded.

Update version number in ./setup.py

Commit and publish to PyPI:
    git commit
    ./setup.py sdist bdist_wheel upload

